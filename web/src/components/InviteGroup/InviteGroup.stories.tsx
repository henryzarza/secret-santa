import type { Meta, StoryObj } from '@storybook/react'

import InviteGroup from './'

const meta: Meta<typeof InviteGroup> = {
  component: InviteGroup,
}

export default meta

type Story = StoryObj<typeof InviteGroup>

export const Primary: Story = {}
